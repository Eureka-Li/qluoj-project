#include<stdio.h>
int main()
{
    int a[110];
    int M,N;
    scanf("%d %d",&M,&N);
    while(M--)
    {
        for(int i=0;i<N;i++)
        {
            scanf("%d",&a[i]);
        }
        for(int i=0;i<N-1;i++)
        {
            for(int j=0;j<N-i-1;j++)
            {
                if(a[j]>a[j+1])
                {
                    int tmp=a[j];
                    a[j]=a[j+1];
                    a[j+1]=tmp;
                }
            }
        }
        for(int j=0;j<N;j++)
        {
            if(j>0) printf(" ");
            printf("%d",a[j]);
        }
        printf("\n");
    }
    return 0;
}