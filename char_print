// https://oj.qlu.edu.cn/p/P2591
// 题目描述

// 在编程竞赛的训练中，学员需要完成一个基础的字符串输出练习。给定一个长度不超过100的字符串，你需要编写程序将其原样输出。这个任务看似简单，但要求精确匹配输出格式，包括所有空格和标点符号。
// 输入格式

// 共一行，包含一个字符串。
// 输出格式

// 共一行，表示输出的字符串。
#include <iostream>
#include <string>
using namespace std;
int main()
{
    string s;
    getline(cin, s);
    cout << s << endl;
    return 0;
}