//你需要编写一个程序来生成一个特殊的数字金字塔。给定一个整数n，金字塔的上半部分由1到n的数字构成，每行数字先递增后递减；下半部分则是上半部分的镜像。金字塔的每一行都居中对齐，形成一个对称的图案。
//https://oj.qlu.edu.cn/p/P1361
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    // 打印金字塔的上半部分
    for (int i = 1; i <= n; i++) {
        // 打印前导空格
        for (int j = 0; j < n - i; j++) {
            printf(" ");
        }
        // 打印递增数字
        for (int j = 1; j <= i; j++) {
            printf("%d", j);
        }
        // 打印递减数字
        for (int j = i - 1; j >= 1; j--) {
            printf("%d", j);
        }
        printf("\n");
    }

    // 打印金字塔的下半部分（镜像上半部分）
    for (int i = n - 1; i >= 1; i--) {
        // 打印前导空格
        for (int j = 0; j < n - i; j++) {
            printf(" ");
        }
        // 打印递增数字
        for (int j = 1; j <= i; j++) {
            printf("%d", j);
        }
        // 打印递减数字
        for (int j = i - 1; j >= 1; j--) {
            printf("%d", j);
        }
        printf("\n");
    }

    return 0;
}