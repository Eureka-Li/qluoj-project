//https://oj.qlu.edu.cn/p/P1496
#include <stdio.h>

int n;
int perm[9];        // 存放当前排列
int used[10];       // 标记数字是否已被使用（1~9）

void backtrack(int depth) {
    if (depth == n) {
        // 输出当前排列
        for (int i = 0; i < n; i++) {
            printf("%d", perm[i]);
        }
        printf("\n");
        return;
    }

    for (int i = 1; i <= n; i++) {
        if (!used[i]) {
            perm[depth] = i;
            used[i] = 1;
            backtrack(depth + 1);
            used[i] = 0;  // 回溯
        }
    }
}

int main() {
    scanf("%d", &n);
    backtrack(0);
    return 0;
}